@using System.Collections.Generic
@using Microsoft.AspNetCore.Http
@using Microsoft.AspNetCore.Http.Authentication
@model InstallationViewModel

@{
    Layout = "_Session";
    ViewData["Title"] = "Installation";
}


<div class="col-xs-10 col-xs-offset-1 col-sm-6 col-sm-offset-3 col-md-4 col-md-offset-4">
    <form asp-controller="Install" asp-action="Index" method="post" role="form" class="form-layout">
        <p class="text-center mb30">Create the initial user account.</p>
        <div asp-validation-summary="All" class="text-danger"></div>
        <div class="form-inputs">
            <input asp-for="UserName" placeholder="Username" class="form-control input-lg" />
            <span asp-validation-for="UserName" class="text-danger"></span>
            <input asp-for="Password" placeholder="Password" type="password" class="form-control input-lg" />
            <span asp-validation-for="Password" class="text-danger"></span>
        </div>
        <button class="btn btn-success btn-block btn-lg mb15" type="submit">
            <span>Install</span>
        </button>
    </form>
</div>

@section styles {
}

@section Scripts {
    @{ await Html.RenderPartialAsync("_ValidationScriptsPartial"); }
}
